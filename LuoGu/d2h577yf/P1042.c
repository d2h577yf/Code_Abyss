#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
  int **arr = malloc(2501 * sizeof(char *));
  for (int i = 0; i < 2501; i++) {
    *arr = malloc(26 * sizeof(char));
  }

  for (int i = 0; i < ; inc - expression) {
  }

  return EXIT_SUCCESS;
}
